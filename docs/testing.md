# Testing

Run backend tests and lint from `backend`, frontend build from `frontend`, and the CLI from the repository root. Do not record a passing result unless the command completed successfully. Docker health checks cover PostgreSQL and FastAPI.

